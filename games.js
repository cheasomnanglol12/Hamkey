// ... (rest of the script remains the same)

async function displayPromoCode(gameKey) {
  // ... (rest of the function remains the same)
  return promoCode;
}

module.exports = { displayPromoCode };
